const lightTheme = {
  backgroundColor: '#ffffff',
  textColor: '#000000',
};

const darkTheme = {
  backgroundColor: '#222222',
  textColor: '#ffffff',
};

const themes = {
  light: lightTheme,
  dark: darkTheme,
};

export default themes;
